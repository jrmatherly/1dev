---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./securitypolicy.yaml
  # The SecurityPolicy targets the existing LiteLLM HTTPRoute in the cluster.
  # No additional HTTPRoute is needed. See httproute.yaml for details.
